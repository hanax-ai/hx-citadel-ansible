---
# Component 10: CopilotKit Adapter - Handlers

- name: Restart orchestrator
  ansible.builtin.systemd:
    name: orchestrator
    state: restarted
    daemon_reload: true
  become: true
  listen: restart orchestrator
