---
# Component 3: PostgreSQL Integration - Handlers

- name: Restart orchestrator
  ansible.builtin.systemd:
    name: shield-orchestrator
    state: restarted
    daemon_reload: true
  become: true
