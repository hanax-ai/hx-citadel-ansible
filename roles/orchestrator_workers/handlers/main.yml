---
# Handlers for orchestrator_workers role

- name: Restart orchestrator
  ansible.builtin.systemd:
    name: shield-orchestrator.service
    state: restarted
    daemon_reload: true
  become: true
