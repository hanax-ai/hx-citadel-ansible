---
# Handlers for orchestrator_qdrant role

- name: Restart orchestrator
  ansible.builtin.systemd:
    name: shield-orchestrator
    state: restarted
    daemon_reload: true
  become: true
  tags: [services]
