---
# Handlers for orchestrator_redis role
- name: Restart orchestrator
  ansible.builtin.systemd:
    name: shield-orchestrator
    state: restarted
    daemon_reload: true
  become: true
  listen: restart orchestrator
